apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: browser

resources:
  - migrate.yaml
  - server.yaml
  - relay.yaml
  - service.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: gh-pool
    includeSelectors: false
