"""Схема и доступ к базе.

adopt() зовётся здесь, а не в точках входа, потому что до базы добирается и
alembic, и оба процесса, и тесты — и каждый своим путём. Прочитать старое имя
DATABASE_URL нужно раньше первого engine(), где бы этот первый вызов ни
случился.
"""

from gh_pool.config import adopt

adopt()
