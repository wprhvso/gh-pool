[project]
name = "gh-chrome"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["gh-chrome-protocol", "httpx>=0.28"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gh_chrome"]
